dbt_project:
  outputs:
    dev:
      dbname: ${{ secrets.DB_NAME }}
      host: ${{ secrets.DB_HOST }}
      pass: ${{ secrets.DB_PASS }}
      port: 6432
      schema: dbt_objects
      threads: 4
      type: postgres
      user: ${{ secrets.DB_USER }}
  target: dev
