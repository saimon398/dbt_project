{% docs s_subscription %}
#### Описание модели
Таблица-сателлит "Подписка". Хранит атрибуты подписки (тип) из источника LOYALTY. Поддерживает историчность через хэш-разность (`mt_hash_diff`) и временные метки актуальности записи.
{% enddocs %}
