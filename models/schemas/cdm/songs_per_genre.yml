version: 2

models:
  - name: songs_per_genre
    description: "{{ doc('songs_per_genre') }}"
    config:
      contract:
        enforced: true
    columns:
      - name: primary_genre
        description: Жанр
        data_type: varchar(100)
      - name: songs_count
        description: Количество песен
        data_type: smallint
