version: 2

models:
  - name: time_spine_daily
    description: 'Модель для работы над семантическим слоем'
    config:
      materialized: table
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day